com.noname.demo:
  Demo1: 不同线程调用同一对象的加锁方法
  Demo2: 脏读的形成和解决
  Demo3: 概念：一致性
  Demo4: synchronized重入锁
  Demo5: synchronized父子继承关系重入锁
  Demo6: synchronized修饰类本身
  Demo7: synchronized修饰对象
  Demo8: volatile关键字（强制线程使用主内存的值）
  Demo9: volatile只保证可见性，不保证原子性
  Demo10: wait和notify
  Demo11: CountDownLatch的简单使用